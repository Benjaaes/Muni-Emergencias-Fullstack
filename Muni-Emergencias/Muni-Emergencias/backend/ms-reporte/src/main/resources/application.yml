server:
  port: 8082

spring:
  application:
    name: ms-reporte
  datasource:
    url: jdbc:h2:mem:reportesdb
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true